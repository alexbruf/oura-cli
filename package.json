{
  "name": "oura-cli",
  "version": "1.0.0",
  "description": "CLI for Oura Ring API v2 — designed for AI agents",
  "type": "module",
  "bin": {
    "oura": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile oura",
    "build:all": "bun build src/index.ts --compile --outfile oura-macos --target=bun-darwin-arm64 && bun build src/index.ts --compile --outfile oura-linux --target=bun-linux-x64"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
